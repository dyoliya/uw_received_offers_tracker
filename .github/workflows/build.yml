name: Build & Release EXE

on:
  push:
    tags:
      - "v*"   # only run when you push a tag like v1.1.0
  workflow_dispatch: # allow manual trigger

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"  # match your local version

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Create .env file
        shell: pwsh
        run: |
          if (-not (Test-Path -Path "config")) {
            mkdir config
          }
          echo "DROPBOX_UW_OFFERS_TRACKER_ACCESS_TOKEN=${{ secrets.DROPBOX_UW_OFFERS_TRACKER_ACCESS_TOKEN }}" >> config/.env
          echo "DROPBOX_UW_OFFERS_TRACKER_APP_KEY=${{ secrets.DROPBOX_UW_OFFERS_TRACKER_APP_KEY }}" >> config/.env
          echo "DROPBOX_UW_OFFERS_TRACKER_APP_SECRET=${{ secrets.DROPBOX_UW_OFFERS_TRACKER_APP_SECRET }}" >> config/.env
          echo "DROPBOX_UW_OFFERS_TRACKER_REFRESH_TOKEN=${{ secrets.DROPBOX_UW_OFFERS_TRACKER_REFRESH_TOKEN }}" >> config/.env

      - name: Build executable with PyInstaller
        run: |
          pyinstaller --onefile --add-data "config/.env;config" uw_received_offers_tracker.py

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/uw_received_offers_tracker.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
